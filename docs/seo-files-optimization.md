# SEO 文件优化说明

> 更新时间：2026-01-26

## 📁 文件位置

```
shun-js/packages/aibaiban-server/assets/
├── robots.txt      # 搜索引擎爬虫规则
├── sitemap.xml     # 网站地图
└── bing.txt        # Bing 站长验证文件
```

## 🤖 robots.txt 优化

### 优化内容

1. **添加详细注释**：说明每个配置的作用
2. **禁止 API 路径**：`Disallow: /api/` - 防止爬虫访问 API 接口
3. **特定爬虫配置**：
   - Google Bot：无延迟，优先爬取
   - Bing Bot：无延迟
   - 百度爬虫：1秒延迟（防止过度占用带宽）

### 配置说明

```
User-agent: *              # 适用于所有爬虫
Allow: /                   # 允许爬取所有页面
Disallow: /api/           # 禁止爬取 API 接口
Crawl-delay: 1            # 爬虫访问间隔（可选）
```

### 注释掉的可选配置

```
# Disallow: /assets/      # 禁止爬取静态资源
# Disallow: /*.js$        # 禁止爬取 JS 文件
# Disallow: /*.css$       # 禁止爬取 CSS 文件
```

**建议**：
- 如果服务器带宽充足，不需要禁止静态资源
- Google 建议允许爬虫访问 CSS/JS，以便正确渲染页面

## 🗺️ sitemap.xml 优化

### 优化内容

1. **更新日期**：`lastmod` 改为 2026-01-26
2. **添加 /board 页面**：白板应用页面
3. **优化更新频率**：
   - 首页：`weekly`（落地页经常更新）
   - /board：`monthly`（应用页面较稳定）
4. **设置优先级**：
   - 首页：`1.0`（最高优先级）
   - /board：`0.8`（高优先级）
5. **预留扩展位置**：注释中包含未来可能添加的页面

### 页面配置

| 页面 | URL | 优先级 | 更新频率 | 说明 |
|------|-----|--------|----------|------|
| 落地页 | `/` | 1.0 | weekly | SEO 优化主页，最高优先级 |
| 白板页 | `/board` | 0.8 | monthly | 应用主功能页面 |

### 预留的扩展页面

```xml
<!-- 未来可添加 -->
/about      - 关于我们（优先级 0.6）
/help       - 帮助中心（优先级 0.5）
/pricing    - 价格页面（优先级 0.7）
```

## 📊 优先级说明

- **1.0**：最重要页面（首页）
- **0.8**：核心功能页面（/board）
- **0.7**：重要页面（定价、功能介绍）
- **0.6**：次要页面（关于我们）
- **0.5**：支持页面（帮助、文档）

## 🔄 更新频率建议

- **daily**：每日更新的页面（新闻、博客）
- **weekly**：每周更新的页面（落地页营销内容）
- **monthly**：每月更新的页面（应用页面、产品页）
- **yearly**：很少更新的页面（关于我们、条款）

## ✅ 部署后验证

### 1. 验证 robots.txt

访问：https://aibaiban.com/robots.txt

应该能看到完整的配置内容。

### 2. 验证 sitemap.xml

访问：https://aibaiban.com/sitemap.xml

应该能看到 XML 格式的站点地图。

### 3. Google Search Console

1. 登录 [Google Search Console](https://search.google.com/search-console)
2. 选择你的网站
3. 左侧菜单 → 站点地图
4. 提交 sitemap.xml：`https://aibaiban.com/sitemap.xml`
5. 等待 Google 索引

### 4. Bing Webmaster Tools

1. 登录 [Bing Webmaster Tools](https://www.bing.com/webmasters)
2. 添加站点，使用 bing.txt 验证
3. 提交站点地图：`https://aibaiban.com/sitemap.xml`

### 5. 测试爬虫抓取

使用 Google 的测试工具：
```
https://search.google.com/test/robots-txt
```

输入你的 robots.txt URL，测试爬虫规则是否正确。

## 🎯 最佳实践

1. **定期更新 sitemap.xml**：
   - 添加新页面时，更新 sitemap.xml
   - 更新 `lastmod` 日期为最后修改日期

2. **监控爬虫行为**：
   - 在 Google Search Console 查看爬取统计
   - 如果爬虫占用带宽过多，调整 `Crawl-delay`

3. **保持 robots.txt 简洁**：
   - 只添加必要的规则
   - 避免过度限制爬虫

4. **优先级分配合理**：
   - 重要页面设置高优先级
   - 不要所有页面都是 1.0

## 📝 自动化建议

### 方案 1：手动更新

每次添加新页面时，手动更新 sitemap.xml。

### 方案 2：动态生成

在服务器端动态生成 sitemap.xml：

```javascript
// 示例：Node.js 动态生成 sitemap
app.get('/sitemap.xml', (req, res) => {
  const urls = [
    { loc: '/', priority: 1.0, changefreq: 'weekly' },
    { loc: '/board', priority: 0.8, changefreq: 'monthly' },
  ];

  const sitemap = generateSitemap(urls);
  res.header('Content-Type', 'application/xml');
  res.send(sitemap);
});
```

### 方案 3：使用工具

- [sitemap-generator](https://www.npmjs.com/package/sitemap-generator)
- [next-sitemap](https://www.npmjs.com/package/next-sitemap)（如果使用 Next.js）

## 🔗 相关资源

- [Google 搜索中心 - robots.txt](https://developers.google.com/search/docs/crawling-indexing/robots/intro)
- [Google 搜索中心 - 站点地图](https://developers.google.com/search/docs/crawling-indexing/sitemaps/overview)
- [Bing Webmaster 指南](https://www.bing.com/webmasters/help/webmaster-guidelines-30fba23a)

---

**维护者**：Vincent
**最后更新**：2026-01-26
